// Backend API + socket.io URL. Override by creating a `.env` file with
// VITE_API_URL=http://localhost:3000 (see .env.example).
export const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:3000';
